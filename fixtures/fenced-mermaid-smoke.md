# Fenced blocks + Mermaid smoke

## Python

```python
def hello():
    return "world"
```

## Noisy Mermaid tag (first token must be mermaid)

```mermaid {.line-numbers}
flowchart LR
  A --> B
```

## Empty Mermaid

```mermaid
```

## Plain text fence

```text
line one
line two
```
